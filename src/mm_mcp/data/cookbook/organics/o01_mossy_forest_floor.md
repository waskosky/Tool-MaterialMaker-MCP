# o01_mossy_forest_floor - Mossy forest floor

_Category: organics. Open the graph: `cookbook/organics/o01_mossy_forest_floor.ptex`._

Prompt: "mossy forest floor". A Phase-3 hero material (frozen 15-case test
set), folded into the cookbook 2026-09-05 so it carries the same gates, card,
and subgraph grouping as every other material.

## Recipe

Clone `dry_earth`, whose cracked-plate ground already has a working
crack-and-plate relief chain, and recolor only its earth albedo ramp
(the donor's `colorize_0`): plate tops become moss green, crack floors become
dark soil, so the same plate topology reads as a mossy floor instead of dried
mud. The lesson that later became the terrain set's rule ("pick the base by
topology, not by donor name"): a connected crack network is the right base
for anything that grows in patches between low seams.

## Subgraph structure

Same two-group split as the other dry_earth-derived materials
(`gl01_frosted_glass`, the terrain plates). Opening the graph shows 5
top-level nodes instead of 13:

- **Ground Color** (`PlateCells`, `PlateEdges`, `PlateWarp`, `MossColor`,
  `AlbedoComposite`, `MossMask`). Exposed: `Plate size`, `Moss and soil
  colors`, `Crack contrast`, `Plate warp`.
- **Ground Relief** (`ReliefRamp`, `ReliefComposite`, `ReliefHeight`,
  `GroundNormal`). Exposed: `Relief strength`.

`LitterNoise` and `MossCoverageNoise` stay top-level: each feeds both groups,
so folding either in would only relabel the sharing as an extra boundary
port.

## See also

`guide://authoring` (or `docs/AUTHORING.md`), "Cross-material lessons" for
the topology-not-donor rule; `docs/evidence/phase3/2026-08-26-iter1.md`.

<!-- nodes:begin -->
## Nodes

Generated by `python -m quality.promote_cookbook` from the shipped graph;
do not edit by hand. Open the `.ptex` and look for these names.

| Subgraph | Node | Type |
|---|---|---|
| (top level) | MossCoverageNoise | perlin |
| (top level) | LitterNoise | perlin |
| (top level) | ground_color | graph |
| (top level) | ground_relief | graph |
| ground_color | PlateCells | voronoi |
| ground_color | PlateEdges | colorize |
| ground_color | MossColor | colorize |
| ground_color | MossMask | colorize |
| ground_color | AlbedoComposite | blend |
| ground_color | PlateWarp | warp |
| ground_relief | ReliefComposite | blend |
| ground_relief | GroundNormal | normal_map |
| ground_relief | ReliefRamp | colorize |
| ground_relief | ReliefHeight | colorize |
<!-- nodes:end -->
