# Proposed 0.8.0a1

This review candidate changes the export, state and native-mutation contracts.
See `docs/upgrade/CHANGE_MAP.md` for source/test traceability and `MIGRATION.md` for
breaking behavior. It adds a persistent shared service, immutable verified builds,
24 high-level tools, a rewritten browser, recipe/context/composition/mask workflows,
authenticated local transports, native-write capability gating, expanded tests,
synchronized wheel resources and developer acceptance scripts.

Executed portable result: 218 passed, 3 skipped. Native engine, real MCP dependency,
WebGL shading and downstream engine imports are not certified by that result.
